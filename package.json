
{
  "name": "rre-kite-backend",
  "version": "2.0.0",
  "description": "Kite Connect backend (Render-ready) with stable proxy, session and token handling",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "undici": "^6.19.8"
  }
}
